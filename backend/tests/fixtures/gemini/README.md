# Synthetic adapter recordings

`valid-response.json` is an authored SDK HTTP-response fixture containing the
existing three evaluation candidates. It is not a captured live Gemini result.
`refusal.json` and `injection.txt` are authored adversarial fixtures.
No credentials, provider request IDs or real personal data are included.
