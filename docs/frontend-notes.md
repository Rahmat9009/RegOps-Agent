# Deprecated frontend-notes location

Do not record frontend contract requests here. Claude owns `frontend/` only and records
requests in `frontend/CONTRACT_REQUESTS.md`. Codex reviews those requests, updates
`contracts/openapi.yaml` when approved, and announces deliberate changes in
`docs/backend-notes.md`.
